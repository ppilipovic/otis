version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Unit tests
    task:
      jobs:
        - name: running integration tests
          commands:
            - running integration tests
      prologue:
        commands:
          - checkout
  - name: Tests
    task:
      jobs:
        - name: 'Job #1'
          commands: []
